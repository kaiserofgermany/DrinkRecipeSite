extends layout

block content
  h1= title
 
  ul
    //drink_list.sort(function(a,b) { let textA = a.title.toUpperCase(); let textB = b.title.toUpperCase(); return (textA < textB) ? -1 : (textA > textB) ? 1 : 0;});
    each drink in drink_list
      li
       a(href=drink.url) #{drink.name}
       
    else
      li There are no drinks
